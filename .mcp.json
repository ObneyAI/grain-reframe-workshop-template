{
  "mcpServers": {
    "nrepl": {
      "type": "stdio",
      "command": "bunx",
      "args": ["nrepl-mcp-server"],
      "env": {}
    },
    "playwright": {
      "type": "stdio",
      "command": "bunx",
      "args": ["@playwright/mcp@latest", "--headless", "--isolated", "--no-sandbox"],
      "env": {}
    }
  }
}
