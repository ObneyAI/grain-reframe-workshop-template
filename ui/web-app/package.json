{
  "name": "@grain/web-app",
  "private": true,
  "scripts": {
    "dev": "concurrently -k -n cljs,css 'bunx --bun --no-install shadow-cljs watch app' 'bun run css:dev'",
    "css:dev": "bunx --bun --no-install @tailwindcss/cli -i ./src/css/main.css -o ../../bases/web-api/resources/public/css/main.css --watch",
    "build": "bun run css:build && bunx --bun --no-install shadow-cljs release app",
    "css:build": "bunx --bun --no-install @tailwindcss/cli -i ./src/css/main.css -o ../../bases/web-api/resources/public/css/main.css --minify",
    "test": "bunx --bun --no-install shadow-cljs compile test && bun out/test/node-tests.js"
  },
  "devDependencies": {
    "@tailwindcss/cli": "^4.1.12",
    "concurrently": "^9.2.0",
    "shadow-cljs": "3.2.0",
    "tailwindcss": "^4.1.12"
  },
  "dependencies": {
    "@grain/shadcn": "*",
    "react": "^19.1.0",
    "react-dom": "^19.1.0"
  }
}
